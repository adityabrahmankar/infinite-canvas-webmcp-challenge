{
  "name": "infinite-canvas-webmcp-challenge",
  "version": "1.0.0",
  "private": false,
  "description": "A focused WebMCP challenge demo: a shared human+agent infinite canvas with structured tools and deterministic export.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:worker": "pnpm dlx wrangler dev --port 8787",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "tsx --test test/*.test.ts",
    "deploy": "pnpm run build && pnpm dlx wrangler deploy"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.7.3",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "agents": "^0.22.0",
    "thinking-orbs": "^0.3.1"
  }
}
